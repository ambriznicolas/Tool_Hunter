import productList from "../ProductListEx.js";
// async function LowHigh(productList) {
//   productList.map((p) => (p.price = p.price.replace(/[^-.\d]/g, "")));

//   productList.sort((a, b) => parseFloat(a.price) - parseFloat(b.price));
//   productList.map((p) => (p.price = "$" + p.price.toString(0, "$")));
//   return productList;
// }
// export default LowHigh;
